machine:
    services:
        - docker
dependencies:
    pre:
        - docker build -t circleci_test:latest .
test:
    override:
        - docker run circleci_test:latest python manage.py test circleci_test
